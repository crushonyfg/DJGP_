# Conformalized Quantile XGBoost

Split conformalized quantile XGBoost on the current UCI small-train presets. Use `aggregate.csv` for train-size summaries and `metrics.csv` for per-seed rows. The conformal interval metrics are direct interval metrics; CRPS is not reported.
